import { BrowserRouter, Routes, Route } from "react-router-dom";
import Platform from "../components/models/platforms/Platform";
export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route>
          
        </Route>
      </Routes>
    </BrowserRouter>
  );
}
